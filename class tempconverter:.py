class tempconverter:
    def __init__(self, kelvin, celsius, fahrenheit):
        self.kelvin=kelvin
        self.celsius=celsius
        self.fahrenheit=fahrenheit
    
    def converter(u_from,u_from_value,u_to,u_to_value):
        u_from= (input("Enter unit you want to convert"))
        u_to= (input("Enter unit you want to convert to:"))
        u_to_value= int(input("Enter temperature in "))
        u_from_value=
        if u_from == 'K' and u_to =='C':
            

        elif u_from == 'C':
        elif u_from == 'F':
        else:
    
        
        
